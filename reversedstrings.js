/*
Prompt:
Complete the solution so that it reverses the string passed into it.
*/

const solution = (str) => {
  return str.split('').reverse().join('');
 }
 
 solution('world');

 /*
 Notes:
 This solution was created a year ago, no notes, but may refactor.
 */